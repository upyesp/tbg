## Agent skills

### Issue tracker

Issues live as GitHub issues in `upyesp/tbg` (via the `gh` CLI).

### Domain docs

Single-context layout — one `CONTEXT.md` + `docs/adr/` at the repo root (to be created when the project takes shape).
